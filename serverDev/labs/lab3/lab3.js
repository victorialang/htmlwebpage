export class lab3 {
  testDefaultParameters(val1, val2=100){
    return var jsonObject = {
      'first':val1,
      'second':val2
    };
  }
   testTemplateLiterals (firstName, middleName, lastName) {
    return var names = `${firstName}, ${middleName}, ${lastName}`;
  }
  testMultilineStrings () {
    return var multilineString = `This is
    a multiline
    string.
    So cool!`;
  }
  testSortWithArrowFunction(arr[num, num1, num2, num3]) {
    return numbers=testSortWithArrowFunction.sort(
      value => `${value}`
    ) ;
  }
}




// middleName=> `${middleName} ,`
// lastName => `${lastName}`;
